{
  "name": "rivet-backend",
  "version": "1.0.0",
  "description": "Backend API para RiVet",
  "scripts": {
    "build": "prisma generate && tsc --skipLibCheck --noEmit false",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "node dist/utils/seed.js"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^8.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@prisma/client": "^5.14.0",
    "bcryptjs": "^2.4.3",
    "fastify": "^4.28.1",
    "tsx": "^4.15.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^20.14.2",
    "prisma": "^5.14.0",
    "typescript": "^5.4.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
